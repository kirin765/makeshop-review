# 메이크샵 리뷰 앱 — 최소 앱 스펙

> **이건 제품이 아니라 계측기다.** 재는 것은 "메이크샵 샵스토어가 이 각도에 유입을 만드는가" 하나다.
> 원장 트랙 "카페24 리뷰 앱"의 파생 — 카페24·고도몰·아임웹 이후 네 번째 포팅.
> 근거 실측: `brain/reports/cafe24-appstore-shelf-2026-07-28.md`

## 게이트 (개발 착수 전 확정, 수정 금지)

| | |
|---|---|
| 출시 마감 | **2026-09-15.** 넘기면 KILL |
| 게이트 | **2026-12-15** |
| kill | **유료 전환 < 10곳** |
| 분모 | 앱스토어 노출 3개월 — 게이트에서 설치 수를 함께 기록 |

## 누구에게 파는가

**외부몰(쿠팡·스마트스토어)에 리뷰가 쌓여 있고, 그걸 메이크샵 자사몰로 옮기고 싶은 소형·초기 셀러.**

카페24 앱스토어에서 실측된 세그먼트를 그대로 가져온다. 넥젠리뷰가 2026-06 진입해 2개월에 리뷰 16건을 받았고,
같은 코호트 2등이 7건이라 이상치다. 메이크샵 스토어에도 같은 통증이 있다고 가정하고 설치 수로 검증한다.

**차별 축은 가격이 아니라 응대 속도와 가벼움.** 실측된 반복 불만 셋이 근거다.
- "화면이 너무 어지럽네요" (스냅리뷰 ★3.7)
- "불필요한 기능들이 많고 무거워서 상세페이지 업로드 속도가 늦은감이 있습니다"
- "문의했을때 답변과 수정이 느려서 힘든적이 많..."

## 가격

**무료체험 14일 → 유료(파트너 결제) 월 9,900원.** 원래 무료 판정이었으나, 2026-08-27
메이크샵 파트너센터의 반려로 유료앱 전환이 강제됐다 — 파트너센터 무료체험은 **기간 기준**만
제공되고 유료앱은 **결제/환불 콜백 API 연동**(수익쉐어 20%)이 심사 필수다.
앱 자체의 무료 20건 카운터는 **무료체험 기간 안에서만** 귀사 정책으로 유지한다.
게이트가 세는 것은 설치 수이고, 수익은 파트너 결제(월 9,900원)로 들어온다.

## 범위 — 넣는 것

| # | 기능 | 메이크샵 구현 |
|---|---|---|
| 1 | 앱 설치·인증 | **hmac 검증이 곧 인증.** OAuth가 없다 |
| 2 | 상품 목록 조회 | `/api/v1/{shopId}/product` + `fields=uid,product_name` |
| 3 | **구매평 엑셀 업로드 → 파싱** | 핵심 가치. 스마트스토어·쿠팡 모두 |
| 4 | 수동 매핑 (외부몰 상품 ↔ 메이크샵 상품) | 드롭다운 선택 |
| 5 | **개인정보 마스킹** | 작성자명 부분 마스킹 |
| 6 | 리뷰 노출 | **후기 API로 등록하면 상품 상세 후기 탭에 네이티브 노출** — 위젯 스크립트 불필요 |
| 7 | 무료체험 14일(기간) → 만료 402 | 설치 시 `apps`의 expired_at 캐시, 만료 시 paywall. 체험 중 무료 20건 유지 |

## 메이크샵과 다른 점 — 실측 (2026-08-13, 공식 문서)

| # | 카페24 (원본) | 메이크샵 |
|---|---|---|
| 1 | OAuth 인가코드 → 토큰(2h/2w) | **client_credentials, 5분 유효, (shop,IP)당 1분 5회** → 반드시 캐시 |
| 2 | 앱 실행 hmac: 정렬 쿼리스트링 base64 | **`{shop_uid}:{timestamp}:{action_type}` hex**, timestamp **밀리초**, ±5분 |
| 3 | 토큰 저장 + 리프레시 | **리프레시 없음.** 만료 시 재발급, DB 캐시로 rate limit 회피 |
| 4 | 게시판 쓰기 `requests[]` 배치 10 | **후기 1건/호출** `/review/store`, 필수 save_type·hname·content |
| 5 | 스크립트 삽입 API로 위젯 | **삽입 API 없음** — 후기 등록 시 상품 상세에 네이티브 노출 |
| 6 | 앱 삭제 웹훅(90077) | **웹훅 미제공** — 토큰이 5분 자동 만료라 재설치 블로커가 구조적으로 없다 |
| 7 | IP 제한 없음 | **접근 허용 IP 등록 필수**(최대 10개) — Vercel IP 등록 필요 |
| 8 | API 버전 헤더 | 없음 |
| 9 | 앱스토어 유료 결제를 플랫폼이 대행 | **파트너 결제** — 결제는 우리가 받고, 결과를 **콜백 API**(`/api/application/{shop_uid}/callback/payment|refund`, 201)로 전달해야 만료일이 갱신된다. 심사 필수 (2026-08-27 반려 사유) |

## 범위 — 빼는 것 (의도적)

| 뺀 것 | 이유 |
|---|---|
| **자동 크롤링 수집** | 카페24 실측으로 폐기 확정. 엑셀 업로드가 답 |
| 자동 상품 매칭 | 수동 붙여넣기로 충분하다 |
| AI 리뷰 요약·분석 | 우리 축은 가벼움 |
| 별점 필터링 (부정 리뷰 제외) | 표시광고법 논점. 안 만든다 |
| 알림톡·리뷰 유도·적립금 | 리뷰 관리 솔루션의 영역. 우리는 이관만 |
| 정기 자동 이관 | 수동 실행 버튼 하나 |
| 위젯 스크립트 | 메이크샵은 후기 등록으로 상품 상세에 네이티브 노출 |

## 스택

- Next.js (App Router) + Vercel
- DB: Postgres (Neon) — 토큰 캐시·사용량 (`makeshop_token`·`makeshop_usage`, cafe24와 같은 DB 공유)
- 메이크샵 connect.makeshop.co.kr API + client_credentials 토큰
- **홈서버 프록시** (ubuntu-lg 8082 + cloudflared `makeshop-proxy.sajangbu.com`) — egress = 등록 IP

## 운영 아키텍처 (2026-08-13 실측 확정)

```
Vercel (makeshop-review-gamma) ──HTTPS──▶ makeshop-proxy.sajangbu.com (cloudflared)
                                              └─▶ ubuntu-lg:8082 (node proxy)
                                                   └─▶ connect.makeshop.co.kr (egress=61.254.69.43)
```

Vercel egress는 유동 IP라 메이크샵 접근 허용 IP(최대 10개)에 등록 불가 → 홈 공인 IP에서
egress하는 홈서버 프록시를 경유한다 (고도몰 godo-proxy와 동일 패턴, `MAKESHOP_API_BASE` +
`MAKESHOP_PROXY_TOKEN`). 프록시는 `x-proxy-token` 헤더로 인증하며 `/oauth`·`/api` 경로만 통과시킨다.

## 열린 항목

| # | 항목 | 상태 |
|---|---|---|
| 1 | 메이크샵 앱 client_id·client_secret | ✅ `08368787-...` / `.env` (2026-08-13) |
| 2 | **접근 허용 IP** | ⚠️ `61.254.69.43` 재등록 필요 (2026-08-26 LAN 변경으로 egress IP 변경 — 이전 `218.237.176.17`) |
| 3 | APP URL | ✅ `https://makeshop-review-gamma.vercel.app/` |
| 4 | **API egress 프록시** | ✅ 홈서버(ubuntu-lg) 8082 + cloudflared `makeshop-proxy.sajangbu.com` |
| 5 | 테스트 몰 | ✅ `hello765` — 설치·토큰·상품·후기 실측 통과 (2026-08-13) |
| 6 | 후기 `score_1..5` | ✅ **전부 같은 값으로 저장**. score_1만 넣으면 나머지가 0으로 저장돼 평균 별점이 깨짐 (실측) |
| 7 | 후기 `reg_date` 백필 | ✅ 과거 작성일로 저장 확인 (June 날짜 보존) |
| 8 | 후기 조회 30일 제한 | ⚠️ review GET은 30일 윈도우만 조회 가능 — 과거 이관 후엔 API로 확인 불가, 관리자 화면으로 확인 |
| 9 | 메이크샵 심사 소요 기간 | ❓ 무료앱 제출 → 2026-08-27 반려 → 유료앱 전환·재신청 → **2026-08-27 보류(결제/환불 콜백 호출 미감지)** → 콜백 실측(F·G) 후 재심사 대기 |
| 10 | **결제/환불 콜백 연동** | ✅ 코드 완료 (`/api/billing/payment|refund` → 콜백 201 → 만료일 덮어쓰기, admin에 콜백 이력 UI). 🔴 **실측 대기 — 심사 보류 해소 조건** (TEST-GUIDE F·G) |

## 하지 않기로 한 것

**고객이 생기면 그때 슬롯을 배정하거나 죽인다.** 이 앱이 문의 응대·기능 요청·업데이트 주기를 갖기 시작하면
그건 계측기가 아니라 제품이다.
